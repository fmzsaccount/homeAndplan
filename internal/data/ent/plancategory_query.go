// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"gy_home/internal/data/ent/plancategory"
	"gy_home/internal/data/ent/predicate"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PlanCategoryQuery is the builder for querying PlanCategory entities.
type PlanCategoryQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.PlanCategory
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PlanCategoryQuery builder.
func (pcq *PlanCategoryQuery) Where(ps ...predicate.PlanCategory) *PlanCategoryQuery {
	pcq.predicates = append(pcq.predicates, ps...)
	return pcq
}

// Limit adds a limit step to the query.
func (pcq *PlanCategoryQuery) Limit(limit int) *PlanCategoryQuery {
	pcq.limit = &limit
	return pcq
}

// Offset adds an offset step to the query.
func (pcq *PlanCategoryQuery) Offset(offset int) *PlanCategoryQuery {
	pcq.offset = &offset
	return pcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pcq *PlanCategoryQuery) Unique(unique bool) *PlanCategoryQuery {
	pcq.unique = &unique
	return pcq
}

// Order adds an order step to the query.
func (pcq *PlanCategoryQuery) Order(o ...OrderFunc) *PlanCategoryQuery {
	pcq.order = append(pcq.order, o...)
	return pcq
}

// First returns the first PlanCategory entity from the query.
// Returns a *NotFoundError when no PlanCategory was found.
func (pcq *PlanCategoryQuery) First(ctx context.Context) (*PlanCategory, error) {
	nodes, err := pcq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{plancategory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pcq *PlanCategoryQuery) FirstX(ctx context.Context) *PlanCategory {
	node, err := pcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PlanCategory ID from the query.
// Returns a *NotFoundError when no PlanCategory ID was found.
func (pcq *PlanCategoryQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = pcq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{plancategory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pcq *PlanCategoryQuery) FirstIDX(ctx context.Context) int32 {
	id, err := pcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PlanCategory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PlanCategory entity is found.
// Returns a *NotFoundError when no PlanCategory entities are found.
func (pcq *PlanCategoryQuery) Only(ctx context.Context) (*PlanCategory, error) {
	nodes, err := pcq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{plancategory.Label}
	default:
		return nil, &NotSingularError{plancategory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pcq *PlanCategoryQuery) OnlyX(ctx context.Context) *PlanCategory {
	node, err := pcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PlanCategory ID in the query.
// Returns a *NotSingularError when more than one PlanCategory ID is found.
// Returns a *NotFoundError when no entities are found.
func (pcq *PlanCategoryQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = pcq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{plancategory.Label}
	default:
		err = &NotSingularError{plancategory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pcq *PlanCategoryQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := pcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PlanCategories.
func (pcq *PlanCategoryQuery) All(ctx context.Context) ([]*PlanCategory, error) {
	if err := pcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return pcq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (pcq *PlanCategoryQuery) AllX(ctx context.Context) []*PlanCategory {
	nodes, err := pcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PlanCategory IDs.
func (pcq *PlanCategoryQuery) IDs(ctx context.Context) ([]int32, error) {
	var ids []int32
	if err := pcq.Select(plancategory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pcq *PlanCategoryQuery) IDsX(ctx context.Context) []int32 {
	ids, err := pcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pcq *PlanCategoryQuery) Count(ctx context.Context) (int, error) {
	if err := pcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return pcq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (pcq *PlanCategoryQuery) CountX(ctx context.Context) int {
	count, err := pcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pcq *PlanCategoryQuery) Exist(ctx context.Context) (bool, error) {
	if err := pcq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return pcq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (pcq *PlanCategoryQuery) ExistX(ctx context.Context) bool {
	exist, err := pcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PlanCategoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pcq *PlanCategoryQuery) Clone() *PlanCategoryQuery {
	if pcq == nil {
		return nil
	}
	return &PlanCategoryQuery{
		config:     pcq.config,
		limit:      pcq.limit,
		offset:     pcq.offset,
		order:      append([]OrderFunc{}, pcq.order...),
		predicates: append([]predicate.PlanCategory{}, pcq.predicates...),
		// clone intermediate query.
		sql:    pcq.sql.Clone(),
		path:   pcq.path,
		unique: pcq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Sort int32 `json:"sort,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PlanCategory.Query().
//		GroupBy(plancategory.FieldSort).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pcq *PlanCategoryQuery) GroupBy(field string, fields ...string) *PlanCategoryGroupBy {
	grbuild := &PlanCategoryGroupBy{config: pcq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := pcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return pcq.sqlQuery(ctx), nil
	}
	grbuild.label = plancategory.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Sort int32 `json:"sort,omitempty"`
//	}
//
//	client.PlanCategory.Query().
//		Select(plancategory.FieldSort).
//		Scan(ctx, &v)
func (pcq *PlanCategoryQuery) Select(fields ...string) *PlanCategorySelect {
	pcq.fields = append(pcq.fields, fields...)
	selbuild := &PlanCategorySelect{PlanCategoryQuery: pcq}
	selbuild.label = plancategory.Label
	selbuild.flds, selbuild.scan = &pcq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a PlanCategorySelect configured with the given aggregations.
func (pcq *PlanCategoryQuery) Aggregate(fns ...AggregateFunc) *PlanCategorySelect {
	return pcq.Select().Aggregate(fns...)
}

func (pcq *PlanCategoryQuery) prepareQuery(ctx context.Context) error {
	for _, f := range pcq.fields {
		if !plancategory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pcq.path != nil {
		prev, err := pcq.path(ctx)
		if err != nil {
			return err
		}
		pcq.sql = prev
	}
	return nil
}

func (pcq *PlanCategoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PlanCategory, error) {
	var (
		nodes = []*PlanCategory{}
		_spec = pcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PlanCategory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PlanCategory{config: pcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (pcq *PlanCategoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pcq.querySpec()
	_spec.Node.Columns = pcq.fields
	if len(pcq.fields) > 0 {
		_spec.Unique = pcq.unique != nil && *pcq.unique
	}
	return sqlgraph.CountNodes(ctx, pcq.driver, _spec)
}

func (pcq *PlanCategoryQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := pcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (pcq *PlanCategoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   plancategory.Table,
			Columns: plancategory.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt32,
				Column: plancategory.FieldID,
			},
		},
		From:   pcq.sql,
		Unique: true,
	}
	if unique := pcq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := pcq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, plancategory.FieldID)
		for i := range fields {
			if fields[i] != plancategory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pcq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pcq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pcq *PlanCategoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pcq.driver.Dialect())
	t1 := builder.Table(plancategory.Table)
	columns := pcq.fields
	if len(columns) == 0 {
		columns = plancategory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pcq.sql != nil {
		selector = pcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pcq.unique != nil && *pcq.unique {
		selector.Distinct()
	}
	for _, p := range pcq.predicates {
		p(selector)
	}
	for _, p := range pcq.order {
		p(selector)
	}
	if offset := pcq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pcq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PlanCategoryGroupBy is the group-by builder for PlanCategory entities.
type PlanCategoryGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pcgb *PlanCategoryGroupBy) Aggregate(fns ...AggregateFunc) *PlanCategoryGroupBy {
	pcgb.fns = append(pcgb.fns, fns...)
	return pcgb
}

// Scan applies the group-by query and scans the result into the given value.
func (pcgb *PlanCategoryGroupBy) Scan(ctx context.Context, v any) error {
	query, err := pcgb.path(ctx)
	if err != nil {
		return err
	}
	pcgb.sql = query
	return pcgb.sqlScan(ctx, v)
}

func (pcgb *PlanCategoryGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range pcgb.fields {
		if !plancategory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := pcgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pcgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (pcgb *PlanCategoryGroupBy) sqlQuery() *sql.Selector {
	selector := pcgb.sql.Select()
	aggregation := make([]string, 0, len(pcgb.fns))
	for _, fn := range pcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(pcgb.fields)+len(pcgb.fns))
		for _, f := range pcgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(pcgb.fields...)...)
}

// PlanCategorySelect is the builder for selecting fields of PlanCategory entities.
type PlanCategorySelect struct {
	*PlanCategoryQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pcs *PlanCategorySelect) Aggregate(fns ...AggregateFunc) *PlanCategorySelect {
	pcs.fns = append(pcs.fns, fns...)
	return pcs
}

// Scan applies the selector query and scans the result into the given value.
func (pcs *PlanCategorySelect) Scan(ctx context.Context, v any) error {
	if err := pcs.prepareQuery(ctx); err != nil {
		return err
	}
	pcs.sql = pcs.PlanCategoryQuery.sqlQuery(ctx)
	return pcs.sqlScan(ctx, v)
}

func (pcs *PlanCategorySelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(pcs.fns))
	for _, fn := range pcs.fns {
		aggregation = append(aggregation, fn(pcs.sql))
	}
	switch n := len(*pcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		pcs.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		pcs.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := pcs.sql.Query()
	if err := pcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
